# Architecture Overview

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND LAYER                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │         eb-web-app (Host) - Port 3000                   │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │         React + Module Federation (Host)           │ │   │
│  │  │  - Main routing and layout                          │ │   │
│  │  │  - Token management (sessionStorage + cookies)     │ │   │
│  │  │  - Auth context provider                           │ │   │
│  │  │  - MUI Theme provider                              │ │   │
│  │  └─────────────────────────────────────────────────────┘ │   │
│  │         ↓                           ↓                      │   │
│  │  ┌──────────────────┐         ┌──────────────────┐        │   │
│  │  │ Events Remote    │         │ Users Remote     │        │   │
│  │  │ Port 3001        │         │ Port 3002        │        │   │
│  │  └──────────────────┘         └──────────────────┘        │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              ↓                                    │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ↓ (HTTP/GraphQL)
┌─────────────────────────────────────────────────────────────────┐
│                       GATEWAY LAYER                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  eb-api-gateway (Apollo Router) - Port 4000             │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │  Apollo Federation Gateway                          │ │   │
│  │  │  - Schema composition                               │ │   │
│  │  │  - Request routing to subgraphs                     │ │   │
│  │  │  - JWT authentication                              │ │   │
│  │  │  - Input validation (class-validator)              │ │   │
│  │  │  - Cross-cutting concerns (auth, monitoring)       │ │   │
│  │  └─────────────────────────────────────────────────────┘ │   │
│  │         ↓                           ↓                      │   │
│  │  ┌──────────────────┐         ┌──────────────────┐        │   │
│  │  │ Events Subgraph  │         │ Users Subgraph   │        │   │
│  │  │ Port 4001        │         │ Port 4002        │        │   │
│  │  └──────────────────┘         └──────────────────┘        │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
       ↓ (MongoDB Protocol)            ↓ (MongoDB Protocol)
┌──────────────────┐         ┌─────────────────────────┐
│Events Subgraph   │         │Users Subgraph           │
└──────────────────┘         └─────────────────────────┘
       ↓                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                         DATABASE LAYER                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              MongoDB - Port 27017                        │   │
│  │  ┌────────────────────┐    ┌──────────────────────────┐  │   │
│  │  │ eb_events Database │    │ eb_users Database       │  │   │
│  │  │                    │    │                         │  │   │
│  │  │ Collections:       │    │ Collections:            │  │   │
│  │  │ - events           │    │ - users                 │  │   │
│  │  │                    │    │                         │  │   │
│  │  └────────────────────┘    └──────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

## 📊 Data Flow

### User Registration Flow
```
User Input (eb-web-app-users)
    ↓
GraphQL Mutation: register
    ↓
eb-api-gateway (validates input, checks auth)
    ↓
eb-api-users (subgraph)
    ↓
Hash password (bcryptjs)
    ↓
MongoDB (eb_users.users)
    ↓
Generate JWT tokens
    ↓
Return: accessToken, refreshToken, user
    ↓
Client stores: accessToken in sessionStorage, refreshToken in HTTP-only cookie
```

### Event Creation Flow
```
User Input (eb-web-app-events)
    ↓
GraphQL Mutation: createEvent
    ↓
Include Authorization Header (Bearer <token>)
    ↓
eb-api-gateway (verifies JWT, validates input)
    ↓
eb-api-events (subgraph)
    ↓
Validate event fields (class-validator)
    ↓
MongoDB (eb_events.events)
    ↓
Return: created event object
    ↓
Update UI with new event
```

### Event Listing with Filter Flow
```
Request (eb-web-app-events)
    ↓
GraphQL Query: events(filter: {category, status})
    ↓
Include Authorization Header (if needed)
    ↓
eb-api-gateway (composes query for subgraphs)
    ↓
eb-api-events (subgraph)
    ↓
MongoDB Query with filters
    ↓
Return: filtered events array
    ↓
Display in table with sorting by date
```

## 🔐 Authentication Architecture

```
┌─────────────────────────────────────────────────────┐
│ Client (eb-web-app)                                 │
│ - sessionStorage: accessToken                       │
│ - Cookie (HTTP-only): refreshToken                  │
└──────────────────┬──────────────────────────────────┘
                   │ Authorization: Bearer <accessToken>
                   ↓
        ┌──────────────────────────┐
        │ eb-api-gateway           │
        │ ┌──────────────────────┐ │
        │ │ JWT Strategy         │ │
        │ │ - Extract from header│ │
        │ │ - Verify signature   │ │
        │ │ - Decode payload     │ │
        │ └──────────────────────┘ │
        │ ┌──────────────────────┐ │
        │ │ GqlAuthGuard         │ │
        │ │ - Apply to resolvers │ │
        │ │ - Check user context │ │
        │ └──────────────────────┘ │
        └──────────────────────────┘
                   │
                   ↓ (if protected mutation/query)
        Routes request to appropriate subgraph
                   │
                   ↓
        ┌──────────────────────────┐
        │ Subgraph Resolver        │
        │ @UseGuards(GqlAuthGuard) │
        │ - Access context.req.user│
        │ - Perform operation      │
        └──────────────────────────┘
```

## 📦 Module Federation Architecture

```
┌─────────────────────────────────────────────┐
│ Webpack Module Federation Configuration     │
├─────────────────────────────────────────────┤
│                                              │
│ Host (eb-web-app)                           │
│ remotes: {                                   │
│   '@ebWebAppEvents': 'remoteEntry.js:4001', │
│   '@ebWebAppUsers': 'remoteEntry.js:4002'   │
│ }                                            │
│ shared: {                                    │
│   react: { singleton: true },               │
│   'react-dom': { singleton: true },         │
│   '@mui/material': { singleton: true }      │
│ }                                            │
│                                              │
│ Remote 1: eb-web-app-events                 │
│ exposes: {                                   │
│   './EventsApp': './src/EventsApp'          │
│ }                                            │
│                                              │
│ Remote 2: eb-web-app-users                  │
│ exposes: {                                   │
│   './UsersApp': './src/UsersApp'            │
│ }                                            │
│                                              │
└─────────────────────────────────────────────┘
```

## 🏛️ Microservices Communication

### Inter-service Communication
```
eb-api-gateway → eb-api-events
  - HTTP requests
  - URL: http://eb-api-events:4001/graphql
  - Introspection polling every 10 seconds
  
eb-api-gateway → eb-api-users
  - HTTP requests
  - URL: http://eb-api-users:4002/graphql
  - Introspection polling every 10 seconds
```

### Service Dependencies (Docker Compose)
```
┌─────────────────────────┐
│ All Backend Services    │
├─────────────────────────┤
│ depends_on:             │
│   mongodb:              │
│     condition:          │
│       service_healthy   │
└─────────────────────────┘
         ↓
┌─────────────────────────┐
│ Frontend Services       │
├─────────────────────────┤
│ depends_on:             │
│   eb-api-gateway        │
└─────────────────────────┘
```

## 🔄 GraphQL Federation Composition

```
┌─────────────────────────────────────────────┐
│ eb-api-gateway (Apollo Gateway)             │
├─────────────────────────────────────────────┤
│                                              │
│ Composed Supergraph includes:                │
│                                              │
│ type Query {                                 │
│   events(...): [Event]        # from events  │
│   event(id): Event            # from events  │
│   user(id): User              # from users   │
│ }                                            │
│                                              │
│ type Mutation {                              │
│   createEvent(...): Event     # from events  │
│   updateEvent(...): Event     # from events  │
│   deleteEvent(id): Event      # from events  │
│   register(...): AuthResponse # from users   │
│   login(...): AuthResponse    # from users   │
│   updateUser(...): User       # from users   │
│ }                                            │
│                                              │
└─────────────────────────────────────────────┘
```

## 🎯 Request Processing Pipeline

```
1. Client Request
   ├── Frontend makes GraphQL query/mutation
   └── Includes JWT in Authorization header
   
2. API Gateway (eb-api-gateway)
   ├── Receives HTTP POST request
   ├── GraphQL parsing
   ├── JWT Verification (JwtStrategy)
   ├── Input Validation (class-validator)
   ├── Route to appropriate subgraph
   └── Compose response
   
3. Subgraph (eb-api-events or eb-api-users)
   ├── Resolver execution
   ├── Schema validation
   ├── Database query/mutation
   └── Return data
   
4. Response
   ├── Gateway collects data from subgraphs
   ├── Merges into single response
   └── Return to client
```

---

This architecture provides:
- ✅ Separation of concerns (microservices)
- ✅ Scalability (independent service scaling)
- ✅ Type safety (TypeScript + GraphQL)
- ✅ Security (JWT authentication, input validation)
- ✅ Modularity (Module Federation for frontend)
- ✅ Database isolation (separate databases per domain)
